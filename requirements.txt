# UK place-centric research engine — runtime dependencies.
# PostgreSQL from day one (the observation uniqueness design relies on
# Postgres NULL-in-unique-constraint semantics; PostGIS comes later).

Django>=5.0,<5.3
psycopg[binary]>=3.1
python-dotenv>=1.0
openpyxl>=3.1
